import { withRouter } from "react-router-dom";
import { IdealBankElement, CardNumberElement } from "@stripe/react-stripe-js";

const PaymentForm = () => {
  return IdealBankElement
}

export default withRouter(PaymentForm)